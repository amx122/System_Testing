<Window x:Class="Kursovva.AddUserWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Новий користувач" Height="450" Width="400"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        FontFamily="{materialDesign:MaterialDesignFont}">

    <StackPanel Margin="20">
        <TextBlock Text="Реєстрація" FontSize="22" FontWeight="Bold" Foreground="#D32F2F" Margin="0,0,0,20"/>

        <TextBox x:Name="txtUsername" materialDesign:HintAssist.Hint="Логін" 
                 Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,10"/>

        <TextBox x:Name="txtPassword" materialDesign:HintAssist.Hint="Пароль" 
                 Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,10"/>

        <TextBox x:Name="txtFullName" materialDesign:HintAssist.Hint="ПІБ (Повне ім'я)" 
                 Style="{StaticResource MaterialDesignOutlinedTextBox}" Margin="0,10"/>

        <ComboBox x:Name="cmbRole" materialDesign:HintAssist.Hint="Роль у системі"
                  Style="{StaticResource MaterialDesignOutlinedComboBox}" Margin="0,10">
            <ComboBoxItem Content="Студент" Tag="Student" IsSelected="True"/>
            <ComboBoxItem Content="Викладач" Tag="Teacher"/>
            <ComboBoxItem Content="Адміністратор" Tag="Admin"/>
        </ComboBox>

        <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,30,0,0">
            <Button Content="Скасувати" IsCancel="True" Style="{StaticResource MaterialDesignFlatButton}"/>
            <Button Content="ДОДАТИ" Click="BtnSave_Click" Style="{StaticResource MaterialDesignRaisedButton}" Margin="10,0,0,0"/>
        </StackPanel>
    </StackPanel>
</Window>