<Window x:Class="Kursovva.EditTestWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Редактор" Height="600" Width="800" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <materialDesign:Card Grid.Row="0" Margin="10" Padding="15" Background="#673AB7">
            <DockPanel>
                <TextBlock x:Name="txtTitle" Text="Тест" Foreground="White" FontSize="20" FontWeight="Bold"/>
                <Button Content="+ ПИТАННЯ" Click="BtnAddQuestion_Click" Style="{StaticResource MaterialDesignRaisedButton}" Background="White" Foreground="#673AB7" HorizontalAlignment="Right" DockPanel.Dock="Right"/>
            </DockPanel>
        </materialDesign:Card>
        <DataGrid x:Name="QuestionsGrid" Grid.Row="1" Margin="10" AutoGenerateColumns="False" IsReadOnly="True" CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Текст" Binding="{Binding Text}" Width="*"/>
                <DataGridTemplateColumn Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Видалити" Click="BtnDelete_Click" Tag="{Binding Id}" Style="{StaticResource MaterialDesignOutlinedButton}" Foreground="Red" BorderBrush="Red"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
        <Button Grid.Row="2" Content="Закрити" Click="BtnClose_Click" Style="{StaticResource MaterialDesignFlatButton}" Margin="10"/>
    </Grid>
</Window>